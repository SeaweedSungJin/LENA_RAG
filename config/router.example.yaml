enabled: true
backend: mmengine
threshold: 0.5
device: cuda:0

# Path to the FrozenLlavaSAM mmengine config (only when backend == "mmengine").
config_path: ../configs/llava/frozen_llava_1_5_vicuna_7b_unet_sam_l_refcoco_png.py

# Optional model checkpoints
# Full model checkpoint (optional)
# checkpoint_path: null

# Router-head checkpoint (trained via Deepspeed). Uncomment and set if available.
# router_checkpoint: /path/to/router_head_checkpoint.pth
# checkpoint_path: /path/to/full_model_checkpoint.pth

# When mmengine/xtuner are not available, change backend to "heuristic" or "always_on".
